build:
  gpu: true
  cuda: "12.1"
  python_version: "3.10"

  system_packages:
    - "git"
    - "ffmpeg"
    - "ca-certificates"
    - "libgl1"
    - "libglib2.0-0"

  python_packages:
    - "torch"
    - "torchvision"
    - "numpy"
    - "pillow"
    - "opencv-python-headless"
    - "safetensors"
    - "requests"
    - "pyyaml"
    - "aiohttp"
    - "tqdm"
    - "python-multipart"

  run:
    - git clone --depth=1 https://github.com/comfyanonymous/ComfyUI /src/ComfyUI
    - pip install -r /src/ComfyUI/requirements.txt

predict: "predict.py:Predictor"
